import { BuscarProductosPipe } from './buscar-productos.pipe';

describe('BuscarProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
